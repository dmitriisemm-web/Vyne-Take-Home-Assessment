import noSelectorsOutsideFile from './no-selectors-outside-file.mjs';
import noXpathSelectors from './no-xpath-selectors.mjs';

export default {
  rules: {
    'no-selectors-outside-file': noSelectorsOutsideFile,
    'no-xpath-selectors': noXpathSelectors
  }
};
