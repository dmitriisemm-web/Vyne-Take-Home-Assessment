name: 'Regression Pipeline'

on:
  workflow_dispatch:
    inputs:
      app:
        description: 'App to test'
        required: true
        type: choice
        options:
          - saucedemo
          - jsonplaceholder
      suite:
        description: 'Suite to run'
        required: true
        type: choice
        default: regression
        options:
          - smoke
          - regression
          - all

jobs:
  run-tests:
    name: Run ${{ inputs.app }} - ${{ inputs.suite }}
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.62.0-jammy
    env:
      # The container runs as root, but $HOME defaults to /github/home
      # (owned by a different uid), which breaks Firefox's launch sandbox.
      HOME: /root
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          # v5 defaults this to true, which shells out to yarn before
          # Corepack is enabled below and fails on this repo's Yarn 4.
          package-manager-cache: false

      - name: Install dependencies
        run: corepack enable && yarn install --immutable

      - name: Determine test script
        id: script
        run: |
          if [ "${{ inputs.suite }}" = "all" ]; then
            echo "name=test:${{ inputs.app }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=test:${{ inputs.app }}:${{ inputs.suite }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Run tests
        run: yarn ${{ steps.script.outputs.name }}

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-report-${{ inputs.app }}-${{ inputs.suite }}
          path: playwright-report/
          retention-days: 14
